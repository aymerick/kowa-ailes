ailes theme for kowa
====================

# Install tools

    $ npm install -g grunt-cli
    $ npm install -g bower

# Install dependencies

    $ npm install
    $ bower install

# Build

    $ grunt build
